import React from 'react';

import Content from './content';
import AppLayout from '@/layouts';

const Page: React.FC = () => {
  return (
    <AppLayout>
      <Content />
    </AppLayout>
  );
};

export default Page;
